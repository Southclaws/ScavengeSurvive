; A simple map file just consists of CreateObject lines.
; With a single line of metadata at the top, which indicates
; the draw distance, interior and virtual world.

0, 0, 500

CreateObject(<model>, <x>, <y>, <z>, <rx>, <ry>, <rz>);
CreateObject(<model>, <x>, <y>, <z>, <rx>, <ry>, <rz>);
CreateObject(<model>, <x>, <y>, <z>, <rx>, <ry>, <rz>);
