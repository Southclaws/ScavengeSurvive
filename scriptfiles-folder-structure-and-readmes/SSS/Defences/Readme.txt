This is where constructable defences are stored.
The filename is the integer representations of defence coordinates and rotation (X, Y, Z, RZ)
The file structure consistes of 4 32bit integers:

	def_type - Defence type
	def_mode - Horizontal/Vertical
	def_hitPoints - Amount of hitpoints left before destruction
	def_pass - Passcode if it's a door type