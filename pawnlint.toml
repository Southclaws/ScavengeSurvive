profile = "recommended"
target = "samp"
include = ["gamemodes/ScavengeSurvive.pwn"]
exclude = ["dependencies/**", "filterscripts/**", "misc/**"]
cache = ".pawnlint-cache"

[lint]
warnings-as-errors = false
max-diagnostics = 200

[rules]
infinite-loop = "off"
