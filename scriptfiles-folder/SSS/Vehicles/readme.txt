Player saved vehicles are saved here, seeing as only one vehicle
is saved per player, the file names are the player's names
rather than the location (which is stored inside the file)

The files contain the vehicle's model, damage data and inventory data.